1. Check if any items hold 1 in current bit position
2. Determine steps to get to next 1 bit with rotate and reverse rotate, 
3. Pick rotate with least amount of steps for each item in list for that bit.